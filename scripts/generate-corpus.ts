// Synthetic label generator. Implementation lands in Phase 1 of TODO.md.
// Reads templates, parameterizes them, renders to test-data/labels/,
// writes ground-truth JSON to test-data/ground-truth/.

async function main() {
  // eslint-disable-next-line no-console
  console.log("[gen:corpus] not yet implemented. See TODO.md Phase 1.");
}

main().catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
