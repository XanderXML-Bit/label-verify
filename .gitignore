node_modules/
.next/
out/
dist/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

.vercel
*.log
.DS_Store
.idea/
.vscode/

# Generated outputs that aren't worth checking in
benchmarks/results/scratch/
test-data/labels/_generated_tmp/

# Tesseract language data (downloaded at runtime)
eng.traineddata
*.traineddata

.vercel

# Claude Code agent worktrees (live inside the repo but should not be
# tracked as nested git directories).
.claude/worktrees/

# Per-run bench log transcripts. The *.json/.md sibling files in
# benchmarks/results/ + .review/threshold-calibration-*.{json,md} are
# the durable artifacts.
.review/calibrate-log.txt

audit-report.html
