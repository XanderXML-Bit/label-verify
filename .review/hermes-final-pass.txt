
session_id: 20260512_163600_68e17a
↻ Resumed session 20260512_163600_68e17a (1 user message, 104 total messages)
Final audit: mostly ready, but these are the 5 fixable issues I would clean before a federal reviewer sees it.

1. docs/MODEL-SELECTION.md contradicts production fallback behavior.
   Lines 200-214 say fallback triggers when any field confidence is below 0.55, then immediately say fallback only happens when the primary provider fails. Code only falls back on extractor/API failure: src/lib/verify.ts:185-243. Fix: state plainly “fallback only runs when Gemini errors/times out/rate-limits,” not on low confidence.

2. docs/MODEL-SELECTION.md claims a Tesseract-only degradation path that does not exist.
   Lines 205-210 show T6 → T7b → T1. Runtime does not degrade to Tesseract-only after both providers fail; it rethrows the primary error: src/lib/verify.ts:244-248. Fix: remove T1 from the production chain or implement that path.

3. Government Warning benchmark framing is too rosy in MODEL-SELECTION.
   docs/MODEL-SELECTION.md:151-156 says T6 satisfies Gov-Warning FN ≤10%. But the cited bake-off file shows T6 Gov-Warning FN-rate = 28.6%: benchmarks/results/2026-05-12T05-18-48-405Z.md:13. Later combined-corpus results are better, but this section reads like post-hoc spin. Fix: say the small routine run failed that submetric; the broader 170-image rerun improved it to the README claim.

4. README overclaims “OCR text is not fed into the vision prompt.”
   README.md:131-135 and 184-187 say OCR-as-hint was falsified and “We ship vision-only.” That is true for verifyLabel, but extractOnly still passes ocrText into visionCtx: src/lib/verify.ts:552-570 and fallback:595-599. Fix: either remove ocrText from extractOnly or narrow the README claim to “verification path.”

5. Live PASS sample creates a bad reviewer impression.
   https://label-verify-six.vercel.app pass sample says “every field should match,” but the result shows Producer Country expected USA / found — and Country Of Origin expected USA / found — while still PASSing via inferred domestic producer address. The logic is defensible, but visually it looks like a missing field got waved through. Fix: change sample copy to explicitly say “country inferred from domestic producer address,” or use a cleaner PASS sample where visible country matches.

I would fix these before submission. They are not huge engineering problems; they are trust problems.

session_id: 20260512_163600_68e17a
